[build]
  command = "npm ci --legacy-peer-deps && npm run build"
  publish = ".next"
  environment = { NEXT_PUBLIC_BUILD_ID = "build" }

[functions]
  directory = "netlify/functions"

[[plugins]]
  package = "@netlify/plugin-nextjs"

[dev]
  command = "npm run dev"
  port = 3000
